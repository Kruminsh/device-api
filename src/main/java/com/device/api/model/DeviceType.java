package com.device.api.model;

/**
 * Device Type enum.
 * The order of defined constants is the same as
 * the order in which the corresponding devices are outputted.
 */
public enum DeviceType {
    GATEWAY,
    SWITCH,
    ACCESS_POINT
}
